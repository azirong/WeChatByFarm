const config = {
    port: 3000,

    //数据库配置
    database: {
        DATABASE: 'farmsql',
        USERNAME: 'root',
        PASSWORD: '111111',
        PORT: '3306',
        HOST: 'localhost'
    }
}

module.exports = config